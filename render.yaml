services:
  - type: web
    name: robot-eyes
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: EYES_API_KEY
        value: roboteyes2025
    plan: free
